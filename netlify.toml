[build]
  command = "npm run build"
  publish = "dist"

# Serve static assets directly (don't redirect JS/CSS/PNG requests to index.html)
[[redirects]]
  from = "/assets/*"
  to = "/assets/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200